name: System Tests

on:
  push:
    branches:
      - main
      - master
      - structures_bending
    paths:
      - "*.py"
      - "tests/**"
      - "requirements*.txt"
      - "pyproject.toml"
      - ".github/workflows/system-tests.yml"
  pull_request:
    paths:
      - "*.py"
      - "tests/**"
      - "requirements*.txt"
      - "pyproject.toml"
      - ".github/workflows/system-tests.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: system-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  system:
    name: System tests
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip

      - name: Install test dependencies
        run: python -m pip install -r requirements-dev.txt

      - name: Run system tests
        env:
          MPLBACKEND: Agg
        run: python -m pytest tests/system
